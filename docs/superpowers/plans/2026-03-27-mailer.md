# Mailer App Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 브라우저를 닫아도 Gmail/Outlook SMTP로 메일을 반복 발송하는 작업 관리 웹앱을 만든다.

**Architecture:** React/Vite 프론트(`/Users/sangjun/IdeaProjects/mailer`)가 Supabase Edge Function(`parking` 프로젝트의 `mailer`)을 호출. pg_cron이 매 1분마다 tick을 보내면 Edge Function이 `mail_jobs` 테이블을 확인해 발송 처리.

**Tech Stack:** React 19, Vite 8, plain CSS, Supabase Edge Functions (Deno), denomailer (Deno SMTP), Supabase pg_cron + pg_net, Vercel

---

## File Structure

### Backend — `/Users/sangjun/IdeaProjects/parking/`
| 파일 | 역할 |
|------|------|
| `supabase/migrations/20260327000001_create_mail_jobs.sql` | DB 스키마 |
| `supabase/functions/mailer/db.ts` | Supabase DB 쿼리 함수 |
| `supabase/functions/mailer/smtp.ts` | denomailer SMTP 발송 |
| `supabase/functions/mailer/index.ts` | HTTP 라우팅, 인증, tick 핸들러 |

### Frontend — `/Users/sangjun/IdeaProjects/mailer/`
| 파일 | 역할 |
|------|------|
| `package.json`, `vite.config.js`, `index.html` | 프로젝트 설정 |
| `src/main.jsx` | 앱 진입점 |
| `src/lib/api.js` | Edge Function 호출 |
| `src/index.css` | 전역 스타일 (보라 팔레트) |
| `src/App.jsx` | 비밀번호 게이트 + 작업 목록 상태 관리 |
| `src/components/JobCard.jsx` | 작업 카드 (시작/중지/수정/삭제) |
| `src/components/TagInput.jsx` | 수신자 이메일 태그 입력 |
| `src/components/JobModal.jsx` | 작업 생성/수정 모달 |

---

### Task 1: DB Migration

**Files:**
- Create: `/Users/sangjun/IdeaProjects/parking/supabase/migrations/20260327000001_create_mail_jobs.sql`

- [ ] **Step 1: Create migration file**

```sql
-- supabase/migrations/20260327000001_create_mail_jobs.sql
CREATE TABLE mail_jobs (
  id               UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  name             TEXT        NOT NULL,
  subject          TEXT        NOT NULL,
  body             TEXT        NOT NULL,
  recipients       TEXT[]      NOT NULL DEFAULT '{}',
  sender           TEXT        NOT NULL CHECK (sender IN ('gmail', 'ms')),
  interval_minutes INTEGER     NOT NULL CHECK (interval_minutes >= 1),
  is_active        BOOLEAN     NOT NULL DEFAULT false,
  last_sent_at     TIMESTAMPTZ,
  send_count       INTEGER     NOT NULL DEFAULT 0,
  created_at       TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

- [ ] **Step 2: Apply migration**

```bash
cd /Users/sangjun/IdeaProjects/parking
supabase db push
```

Expected: `Applying migration 20260327000001_create_mail_jobs.sql` — 에러 없음.

- [ ] **Step 3: Verify**

Supabase Dashboard → Table Editor → `mail_jobs` 테이블과 11개 컬럼 확인.

- [ ] **Step 4: Commit**

```bash
cd /Users/sangjun/IdeaProjects/parking
git add supabase/migrations/20260327000001_create_mail_jobs.sql
git commit -m "feat: add mail_jobs table"
```

---

### Task 2: Edge Function — db.ts

**Files:**
- Create: `/Users/sangjun/IdeaProjects/parking/supabase/functions/mailer/db.ts`

- [ ] **Step 1: Create db.ts**

```ts
// supabase/functions/mailer/db.ts
import { createClient } from "jsr:@supabase/supabase-js@2"

export interface MailJob {
  id: string
  name: string
  subject: string
  body: string
  recipients: string[]
  sender: "gmail" | "ms"
  interval_minutes: number
  is_active: boolean
  last_sent_at: string | null
  send_count: number
  created_at: string
}

function getDb() {
  return createClient(
    Deno.env.get("SUPABASE_URL")!,
    Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!,
  )
}

export async function listJobs(): Promise<MailJob[]> {
  const db = getDb()
  const { data, error } = await db
    .from("mail_jobs")
    .select("*")
    .order("created_at", { ascending: false })
  if (error) throw error
  return data ?? []
}

export async function createJob(job: Omit<MailJob, "id" | "is_active" | "last_sent_at" | "send_count" | "created_at">): Promise<MailJob> {
  const db = getDb()
  const { data, error } = await db
    .from("mail_jobs")
    .insert(job)
    .select()
    .single()
  if (error) throw error
  return data
}

export async function updateJob(id: string, patch: Partial<MailJob>): Promise<MailJob> {
  const db = getDb()
  const { data, error } = await db
    .from("mail_jobs")
    .update(patch)
    .eq("id", id)
    .select()
    .single()
  if (error) throw error
  return data
}

export async function deleteJob(id: string): Promise<void> {
  const db = getDb()
  const { error } = await db.from("mail_jobs").delete().eq("id", id)
  if (error) throw error
}

export async function getDueJobs(): Promise<MailJob[]> {
  const db = getDb()
  const { data, error } = await db
    .from("mail_jobs")
    .select("*")
    .eq("is_active", true)
  if (error) throw error
  const now = Date.now()
  return (data ?? []).filter((job: MailJob) => {
    if (!job.last_sent_at) return true
    return now >= new Date(job.last_sent_at).getTime() + job.interval_minutes * 60_000
  })
}

export async function markSent(id: string, currentCount: number): Promise<void> {
  const db = getDb()
  const { error } = await db
    .from("mail_jobs")
    .update({
      last_sent_at: new Date().toISOString(),
      send_count: currentCount + 1,
    })
    .eq("id", id)
  if (error) throw error
}
```

- [ ] **Step 2: Commit**

```bash
cd /Users/sangjun/IdeaProjects/parking
git add supabase/functions/mailer/db.ts
git commit -m "feat: mailer Edge Function db helpers"
```

---

### Task 3: Edge Function — smtp.ts

**Files:**
- Create: `/Users/sangjun/IdeaProjects/parking/supabase/functions/mailer/smtp.ts`

- [ ] **Step 1: Create smtp.ts**

```ts
// supabase/functions/mailer/smtp.ts
import { SMTPClient } from "https://deno.land/x/denomailer@1.6.0/mod.ts"

interface SendOptions {
  sender: "gmail" | "ms"
  to: string
  subject: string
  body: string
}

export async function sendMail(opts: SendOptions): Promise<void> {
  const isGmail = opts.sender === "gmail"
  const user = isGmail
    ? Deno.env.get("GMAIL_USER")!
    : Deno.env.get("MS_USER")!
  const password = isGmail
    ? Deno.env.get("GMAIL_APP_PASSWORD")!
    : Deno.env.get("MS_APP_PASSWORD")!
  const hostname = isGmail ? "smtp.gmail.com" : "smtp.office365.com"

  const client = new SMTPClient({
    connection: {
      hostname,
      port: 587,
      tls: false,
      auth: { username: user, password },
    },
  })

  try {
    await client.send({
      from: user,
      to: opts.to,
      subject: opts.subject,
      content: opts.body,
    })
  } finally {
    await client.close()
  }
}
```

- [ ] **Step 2: Commit**

```bash
cd /Users/sangjun/IdeaProjects/parking
git add supabase/functions/mailer/smtp.ts
git commit -m "feat: mailer SMTP helper (Gmail + Outlook)"
```

---

### Task 4: Edge Function — index.ts

**Files:**
- Create: `/Users/sangjun/IdeaProjects/parking/supabase/functions/mailer/index.ts`

- [ ] **Step 1: Create index.ts**

```ts
// supabase/functions/mailer/index.ts
import { listJobs, createJob, updateJob, deleteJob, getDueJobs, markSent } from "./db.ts"
import { sendMail } from "./smtp.ts"

const corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type, x-app-password",
  "Access-Control-Allow-Methods": "GET, POST, PATCH, DELETE, OPTIONS",
}

function json(data: unknown, status = 200) {
  return new Response(JSON.stringify(data), {
    status,
    headers: { "Content-Type": "application/json", ...corsHeaders },
  })
}

function checkAppPassword(req: Request): boolean {
  return req.headers.get("x-app-password") === Deno.env.get("APP_PASSWORD")
}

Deno.serve(async (req) => {
  if (req.method === "OPTIONS") return new Response(null, { headers: corsHeaders })

  try {
    const url = new URL(req.url)
    const action = url.searchParams.get("action")
    const id = url.searchParams.get("id")

    // POST ?action=tick — pg_cron 호출 (APP_PASSWORD 검증 불필요)
    if (req.method === "POST" && action === "tick") {
      const jobs = await getDueJobs()
      const results = await Promise.allSettled(
        jobs.map(async (job) => {
          for (const recipient of job.recipients) {
            await sendMail({ sender: job.sender, to: recipient, subject: job.subject, body: job.body })
          }
          await markSent(job.id, job.send_count)
        })
      )
      results.forEach((r, i) => {
        if (r.status === "rejected") console.error(`Job ${jobs[i].id} failed:`, r.reason)
      })
      const failed = results.filter(r => r.status === "rejected").length
      return json({ processed: jobs.length, failed })
    }

    // CRUD — APP_PASSWORD 필수
    if (!checkAppPassword(req)) return json({ error: "unauthorized" }, 401)

    // GET — 작업 목록
    if (req.method === "GET") {
      return json(await listJobs())
    }

    // POST — 작업 생성
    if (req.method === "POST") {
      const body = await req.json()
      return json(await createJob(body), 201)
    }

    // PATCH ?id= — 작업 수정
    if (req.method === "PATCH") {
      if (!id) return json({ error: "id required" }, 400)
      const body = await req.json()
      return json(await updateJob(id, body))
    }

    // DELETE ?id= — 작업 삭제
    if (req.method === "DELETE") {
      if (!id) return json({ error: "id required" }, 400)
      await deleteJob(id)
      return json({ success: true })
    }

    return json({ error: "not found" }, 404)
  } catch (e) {
    console.error("mailer error:", e)
    return json({ error: "internal server error", details: e.message }, 500)
  }
})
```

- [ ] **Step 2: Commit**

```bash
cd /Users/sangjun/IdeaProjects/parking
git add supabase/functions/mailer/index.ts
git commit -m "feat: mailer Edge Function routing + tick handler"
```

---

### Task 5: Set Supabase Secrets

**Files:** 없음 (CLI 명령어만)

실제 계정 정보를 Supabase Secrets에 등록한다. 아래 명령어에서 `<값>` 부분을 실제 값으로 교체.

- Gmail 앱 비밀번호: Google 계정 → 보안 → 2단계 인증 → 앱 비밀번호
- Outlook 앱 비밀번호: Microsoft 계정 → 보안 → 고급 보안 → 앱 비밀번호

- [ ] **Step 1: Set secrets**

```bash
cd /Users/sangjun/IdeaProjects/parking
supabase secrets set \
  GMAIL_USER=<gmail주소@gmail.com> \
  GMAIL_APP_PASSWORD=<gmail앱비밀번호> \
  MS_USER=<outlook주소@outlook.com> \
  MS_APP_PASSWORD=<outlook앱비밀번호> \
  APP_PASSWORD=<프론트비밀번호> \
  --project-ref enawzdqroidrhtjqhpka
```

Expected: `Finished supabase secrets set.`

- [ ] **Step 2: Verify secrets are set**

```bash
supabase secrets list --project-ref enawzdqroidrhtjqhpka
```

Expected: `GMAIL_USER`, `GMAIL_APP_PASSWORD`, `MS_USER`, `MS_APP_PASSWORD`, `APP_PASSWORD` 가 목록에 표시됨.

---

### Task 6: Deploy Edge Function + Setup pg_cron

**Files:** 없음 (CLI + Supabase Dashboard)

- [ ] **Step 1: Deploy mailer Edge Function**

```bash
cd /Users/sangjun/IdeaProjects/parking
supabase functions deploy mailer --project-ref enawzdqroidrhtjqhpka
```

Expected: `Deployed Function mailer` — URL 출력됨.

- [ ] **Step 2: Test CRUD endpoint**

```bash
curl -X GET \
  'https://enawzdqroidrhtjqhpka.supabase.co/functions/v1/mailer' \
  -H 'Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVuYXd6ZHFyb2lkcmh0anFocGthIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjkwMjM1MTIsImV4cCI6MjA4NDU5OTUxMn0.3D7Zeop7MzA-AytZw07hCjN7MddeWK_gN4aQY5F3AEg' \
  -H 'x-app-password: <APP_PASSWORD 실제값>'
```

Expected: `[]` (빈 배열)

- [ ] **Step 3: Test tick endpoint**

```bash
curl -X POST \
  'https://enawzdqroidrhtjqhpka.supabase.co/functions/v1/mailer?action=tick' \
  -H 'Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVuYXd6ZHFyb2lkcmh0anFocGthIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjkwMjM1MTIsImV4cCI6MjA4NDU5OTUxMn0.3D7Zeop7MzA-AytZw07hCjN7MddeWK_gN4aQY5F3AEg' \
  -H 'Content-Type: application/json' \
  -d '{}'
```

Expected: `{"processed":0,"failed":0}`

- [ ] **Step 4: Enable pg_cron in Supabase Dashboard**

Supabase Dashboard → Database → Extensions → `pg_cron` 검색 → Enable.
`pg_net`도 같은 방식으로 Enable.

- [ ] **Step 5: Setup pg_cron job**

Supabase Dashboard → SQL Editor → 아래 SQL 실행:

```sql
SELECT cron.schedule(
  'mailer-tick',
  '* * * * *',
  $$SELECT net.http_post(
    url := 'https://enawzdqroidrhtjqhpka.supabase.co/functions/v1/mailer?action=tick',
    body := '{}',
    headers := '{"Authorization": "Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVuYXd6ZHFyb2lkcmh0anFocGthIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjkwMjM1MTIsImV4cCI6MjA4NDU5OTUxMn0.3D7Zeop7MzA-AytZw07hCjN7MddeWK_gN4aQY5F3AEg", "Content-Type": "application/json"}'::jsonb
  )$$
);
```

Expected: `schedule` 열에 숫자가 반환됨.

- [ ] **Step 6: Verify pg_cron is running**

2분 후 SQL Editor에서:

```sql
SELECT * FROM cron.job_run_details ORDER BY start_time DESC LIMIT 5;
```

Expected: `status = 'succeeded'` 인 행이 보임.

---

### Task 7: Scaffold Frontend Project

**Files:**
- Create: `/Users/sangjun/IdeaProjects/mailer/` (새 프로젝트)

- [ ] **Step 1: Create Vite project**

```bash
cd /Users/sangjun/IdeaProjects
npm create vite@latest mailer -- --template react
cd mailer
npm install
npm install lucide-react @tailwindcss/vite tailwindcss
```

- [ ] **Step 2: Replace vite.config.js**

```js
// vite.config.js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
})
```

- [ ] **Step 3: Replace index.html**

```html
<!doctype html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mailer</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

- [ ] **Step 4: Create .env and .env.example**

```
# .env
VITE_MAILER_URL=https://enawzdqroidrhtjqhpka.supabase.co/functions/v1/mailer
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVuYXd6ZHFyb2lkcmh0anFocGthIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjkwMjM1MTIsImV4cCI6MjA4NDU5OTUxMn0.3D7Zeop7MzA-AytZw07hCjN7MddeWK_gN4aQY5F3AEg
```

`.env.example`에 동일한 키, 값은 빈 문자열.

- [ ] **Step 5: Update .gitignore**

```
# .gitignore (기존 내용에 추가)
.env
dist
```

- [ ] **Step 6: Create src/main.jsx**

```jsx
// src/main.jsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.jsx'

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

- [ ] **Step 7: Initialize git and commit**

```bash
cd /Users/sangjun/IdeaProjects/mailer
git init
git add -A
git commit -m "chore: scaffold mailer Vite project"
```

- [ ] **Step 8: Verify dev server starts**

```bash
npm run dev
```

Expected: `http://localhost:5173` 접속 가능, Vite 기본 화면 표시.

---

### Task 8: API Library

**Files:**
- Create: `/Users/sangjun/IdeaProjects/mailer/src/lib/api.js`

- [ ] **Step 1: Create api.js**

```js
// src/lib/api.js
const BASE = import.meta.env.VITE_MAILER_URL
const ANON_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY

async function request(method, path = '', body = null, password) {
  const res = await fetch(`${BASE}${path}`, {
    method,
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${ANON_KEY}`,
      'x-app-password': password ?? '',
    },
    body: body ? JSON.stringify(body) : null,
  })
  if (res.status === 401) throw new Error('UNAUTHORIZED')
  if (!res.ok) {
    const text = await res.text()
    throw new Error(text || `HTTP ${res.status}`)
  }
  if (method === 'DELETE') return null
  return res.json()
}

export const getJobs = (pw) => request('GET', '', null, pw)
export const createJob = (job, pw) => request('POST', '', job, pw)
export const updateJob = (id, patch, pw) => request('PATCH', `?id=${id}`, patch, pw)
export const deleteJob = (id, pw) => request('DELETE', `?id=${id}`, null, pw)
```

- [ ] **Step 2: Commit**

```bash
git add src/lib/api.js
git commit -m "feat: API client for mailer Edge Function"
```

---

### Task 9: Global CSS

**Files:**
- Modify: `/Users/sangjun/IdeaProjects/mailer/src/index.css`

- [ ] **Step 1: Replace index.css**

```css
/* src/index.css */
@import "tailwindcss";

*, *::before, *::after { box-sizing: border-box; }

body {
  margin: 0;
  background: #0d0d14;
  color: #e2e2e2;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  min-height: 100vh;
}

/* ── 비밀번호 게이트 ── */
.gate-wrapper {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #0d0d14;
  background-image: radial-gradient(rgba(157,143,252,0.07) 1px, transparent 1px);
  background-size: 20px 20px;
}
.gate-card {
  width: 100%;
  max-width: 360px;
  margin: 0 16px;
  background: rgba(255,255,255,0.03);
  border: 1px solid rgba(157,143,252,0.2);
  border-radius: 16px;
  padding: 36px 32px;
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
}
.gate-title {
  font-size: 20px;
  font-weight: 700;
  color: #f0f0f0;
  margin: 0 0 6px;
}
.gate-subtitle {
  font-size: 13px;
  color: #606070;
  margin: 0 0 24px;
}
.gate-input {
  width: 100%;
  background: rgba(255,255,255,0.04);
  border: 1px solid rgba(157,143,252,0.2);
  border-radius: 10px;
  padding: 10px 14px;
  font-size: 14px;
  color: #e2e2e2;
  outline: none;
  margin-bottom: 14px;
  transition: border-color 150ms ease;
}
.gate-input:focus { border-color: #9d8ffc; }
.gate-btn {
  width: 100%;
  padding: 11px;
  background: #9d8ffc;
  border: none;
  border-radius: 10px;
  font-size: 14px;
  font-weight: 600;
  color: #fff;
  cursor: pointer;
  transition: background 150ms ease;
}
.gate-btn:hover { background: #b0a4fd; }
.gate-error {
  font-size: 12px;
  color: rgb(248,113,113);
  margin-top: 10px;
  text-align: center;
}

/* ── 앱 레이아웃 ── */
.app { min-height: 100vh; padding: 0 0 40px; }
.app-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 18px 32px;
  border-bottom: 1px solid rgba(157,143,252,0.1);
  background: rgba(255,255,255,0.01);
}
.app-title {
  font-size: 15px;
  font-weight: 800;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: #9d8ffc;
}
.app-new-btn {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 8px 16px;
  background: rgba(157,143,252,0.12);
  border: 1px solid rgba(157,143,252,0.3);
  border-radius: 9px;
  font-size: 13px;
  font-weight: 600;
  color: #9d8ffc;
  cursor: pointer;
  transition: background 150ms ease;
}
.app-new-btn:hover { background: rgba(157,143,252,0.2); }

/* ── 작업 목록 ── */
.job-list {
  max-width: 860px;
  margin: 28px auto;
  padding: 0 24px;
  display: flex;
  flex-direction: column;
  gap: 14px;
}
.job-empty {
  text-align: center;
  color: #404050;
  font-size: 14px;
  padding: 60px 0;
}

/* ── 작업 카드 ── */
.job-card {
  background: rgba(255,255,255,0.02);
  border: 1px solid rgba(157,143,252,0.12);
  border-radius: 14px;
  padding: 18px 20px;
  transition: border-color 150ms ease;
}
.job-card-active {
  border-color: rgba(157,143,252,0.45);
  background: rgba(157,143,252,0.04);
}
.job-card-top {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12px;
  margin-bottom: 10px;
}
.job-card-name {
  font-size: 15px;
  font-weight: 600;
  color: #e8e8f0;
}
.job-card-actions {
  display: flex;
  gap: 8px;
  flex-shrink: 0;
}
.job-card-meta {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  font-size: 12px;
  color: #606070;
}
.job-meta-item { display: flex; align-items: center; gap: 4px; }
.job-badge {
  font-size: 11px;
  font-weight: 700;
  padding: 2px 8px;
  border-radius: 999px;
  letter-spacing: 0.04em;
}
.job-badge-gmail { background: rgba(234,67,53,0.15); color: #f87171; }
.job-badge-ms { background: rgba(0,120,212,0.15); color: #60a5fa; }
.job-status-dot {
  width: 7px; height: 7px;
  border-radius: 50%;
  background: #404050;
  display: inline-block;
}
.job-status-dot-active { background: #9d8ffc; box-shadow: 0 0 6px rgba(157,143,252,0.5); }

/* ── 버튼 ── */
.btn {
  display: flex;
  align-items: center;
  gap: 4px;
  padding: 6px 12px;
  border-radius: 8px;
  font-size: 12px;
  font-weight: 600;
  cursor: pointer;
  border: 1px solid transparent;
  transition: background 150ms ease, color 150ms ease;
}
.btn-start {
  background: rgba(157,143,252,0.1);
  border-color: rgba(157,143,252,0.3);
  color: #9d8ffc;
}
.btn-start:hover { background: rgba(157,143,252,0.2); }
.btn-stop {
  background: rgba(255,255,255,0.05);
  border-color: rgba(255,255,255,0.1);
  color: #a0a0b0;
}
.btn-stop:hover { background: rgba(255,255,255,0.08); }
.btn-edit {
  background: rgba(255,255,255,0.04);
  border-color: rgba(255,255,255,0.08);
  color: #808090;
}
.btn-edit:hover { color: #c0c0d0; background: rgba(255,255,255,0.07); }
.btn-delete {
  background: transparent;
  border-color: transparent;
  color: #505060;
}
.btn-delete:hover { color: rgb(248,113,113); background: rgba(248,113,113,0.1); }

/* ── 모달 ── */
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.6);
  backdrop-filter: blur(4px);
  -webkit-backdrop-filter: blur(4px);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 50;
  padding: 20px;
}
.modal {
  width: 100%;
  max-width: 500px;
  background: #13131f;
  border: 1px solid rgba(157,143,252,0.2);
  border-radius: 16px;
  padding: 28px;
  max-height: 90vh;
  overflow-y: auto;
}
.modal-title {
  font-size: 16px;
  font-weight: 700;
  color: #f0f0f0;
  margin: 0 0 24px;
}
.form-field { margin-bottom: 18px; }
.form-label {
  display: block;
  font-size: 12px;
  font-weight: 600;
  color: #8080a0;
  margin-bottom: 6px;
  text-transform: uppercase;
  letter-spacing: 0.06em;
}
.form-input, .form-textarea, .form-select {
  width: 100%;
  background: rgba(255,255,255,0.04);
  border: 1px solid rgba(157,143,252,0.15);
  border-radius: 9px;
  padding: 9px 12px;
  font-size: 14px;
  color: #e2e2e2;
  outline: none;
  transition: border-color 150ms ease;
  font-family: inherit;
}
.form-input:focus, .form-textarea:focus, .form-select:focus { border-color: #9d8ffc; }
.form-textarea { min-height: 100px; resize: vertical; }
.form-select { cursor: pointer; }

/* 발송 간격 행 */
.interval-row { display: flex; gap: 8px; }
.interval-row .form-input { flex: 1; }
.interval-row .form-select { width: 100px; flex-shrink: 0; }

/* 발신자 라디오 */
.radio-group { display: flex; gap: 12px; }
.radio-label {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 13px;
  color: #a0a0b0;
  cursor: pointer;
}
.radio-label input[type="radio"] { accent-color: #9d8ffc; }

/* 모달 버튼 행 */
.modal-actions { display: flex; gap: 10px; justify-content: flex-end; margin-top: 24px; }
.modal-cancel {
  padding: 9px 20px;
  background: rgba(255,255,255,0.04);
  border: 1px solid rgba(255,255,255,0.1);
  border-radius: 9px;
  font-size: 14px;
  color: #808090;
  cursor: pointer;
}
.modal-cancel:hover { color: #c0c0d0; }
.modal-submit {
  padding: 9px 20px;
  background: #9d8ffc;
  border: none;
  border-radius: 9px;
  font-size: 14px;
  font-weight: 600;
  color: #fff;
  cursor: pointer;
  transition: background 150ms ease;
}
.modal-submit:hover { background: #b0a4fd; }

/* ── 태그 입력 ── */
.tag-input-wrap {
  min-height: 42px;
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 6px;
  background: rgba(255,255,255,0.04);
  border: 1px solid rgba(157,143,252,0.15);
  border-radius: 9px;
  padding: 6px 10px;
  cursor: text;
  transition: border-color 150ms ease;
}
.tag-input-wrap:focus-within { border-color: #9d8ffc; }
.tag-pill {
  display: flex;
  align-items: center;
  gap: 4px;
  background: rgba(157,143,252,0.15);
  border: 1px solid rgba(157,143,252,0.3);
  border-radius: 999px;
  padding: 2px 8px 2px 10px;
  font-size: 12px;
  color: #c0b8ff;
}
.tag-pill-remove {
  background: none;
  border: none;
  color: #9d8ffc;
  cursor: pointer;
  padding: 0;
  font-size: 14px;
  line-height: 1;
  display: flex;
  align-items: center;
}
.tag-pill-remove:hover { color: rgb(248,113,113); }
.tag-input-inner {
  border: none;
  background: transparent;
  outline: none;
  font-size: 13px;
  color: #e2e2e2;
  flex: 1;
  min-width: 120px;
}
.tag-input-inner::placeholder { color: #404050; }
```

- [ ] **Step 2: Commit**

```bash
git add src/index.css
git commit -m "feat: global CSS styles (purple palette)"
```

---

### Task 10: App.jsx — 비밀번호 게이트 + 작업 목록

**Files:**
- Modify: `/Users/sangjun/IdeaProjects/mailer/src/App.jsx`

- [ ] **Step 1: Replace App.jsx**

```jsx
// src/App.jsx
import { useState, useEffect, useCallback } from 'react'
import { Plus } from 'lucide-react'
import { getJobs, createJob, updateJob, deleteJob } from './lib/api.js'
import JobCard from './components/JobCard.jsx'
import JobModal from './components/JobModal.jsx'

const PW_KEY = 'mailer-password'

export default function App() {
  const [password, setPassword] = useState(() => localStorage.getItem(PW_KEY) ?? '')
  const [authenticated, setAuthenticated] = useState(false)
  const [pwInput, setPwInput] = useState('')
  const [pwError, setPwError] = useState('')
  const [jobs, setJobs] = useState([])
  const [loading, setLoading] = useState(false)
  const [showModal, setShowModal] = useState(false)
  const [editJob, setEditJob] = useState(null)

  const fetchJobs = useCallback(async (pw = password) => {
    try {
      const data = await getJobs(pw)
      setJobs(data)
      setAuthenticated(true)
      localStorage.setItem(PW_KEY, pw)
    } catch (e) {
      if (e.message === 'UNAUTHORIZED') {
        setAuthenticated(false)
        localStorage.removeItem(PW_KEY)
      }
    }
  }, [password])

  useEffect(() => {
    if (password) fetchJobs(password)
  }, [])

  const handleLogin = async (e) => {
    e.preventDefault()
    setPwError('')
    try {
      const data = await getJobs(pwInput)
      setJobs(data)
      setPassword(pwInput)
      setAuthenticated(true)
      localStorage.setItem(PW_KEY, pwInput)
    } catch (e) {
      if (e.message === 'UNAUTHORIZED') setPwError('비밀번호가 틀렸습니다.')
      else setPwError('연결 오류가 발생했습니다.')
    }
  }

  const handleCreate = async (formData) => {
    setLoading(true)
    try {
      const job = await createJob(formData, password)
      setJobs(prev => [job, ...prev])
      setShowModal(false)
    } finally {
      setLoading(false)
    }
  }

  const handleUpdate = async (id, patch) => {
    const job = await updateJob(id, patch, password)
    setJobs(prev => prev.map(j => j.id === id ? job : j))
    setEditJob(null)
    setShowModal(false)
  }

  const handleDelete = async (id) => {
    await deleteJob(id, password)
    setJobs(prev => prev.filter(j => j.id !== id))
  }

  const handleToggle = (job) => {
    handleUpdate(job.id, { is_active: !job.is_active })
  }

  if (!authenticated) {
    return (
      <div className="gate-wrapper">
        <div className="gate-card">
          <h1 className="gate-title">Mailer</h1>
          <p className="gate-subtitle">비밀번호를 입력하세요</p>
          <form onSubmit={handleLogin}>
            <input
              className="gate-input"
              type="password"
              value={pwInput}
              onChange={e => setPwInput(e.target.value)}
              placeholder="비밀번호"
              autoFocus
            />
            <button className="gate-btn" type="submit">확인</button>
            {pwError && <p className="gate-error">{pwError}</p>}
          </form>
        </div>
      </div>
    )
  }

  return (
    <div className="app">
      <header className="app-header">
        <span className="app-title">Mailer</span>
        <button className="app-new-btn" onClick={() => { setEditJob(null); setShowModal(true) }}>
          <Plus size={14} /> 새 작업
        </button>
      </header>

      <div className="job-list">
        {jobs.length === 0 ? (
          <p className="job-empty">작업이 없습니다. 새 작업을 만들어보세요.</p>
        ) : (
          jobs.map(job => (
            <JobCard
              key={job.id}
              job={job}
              onToggle={() => handleToggle(job)}
              onEdit={() => { setEditJob(job); setShowModal(true) }}
              onDelete={() => handleDelete(job.id)}
            />
          ))
        )}
      </div>

      {showModal && (
        <JobModal
          job={editJob}
          onSubmit={editJob ? (data) => handleUpdate(editJob.id, data) : handleCreate}
          onClose={() => { setShowModal(false); setEditJob(null) }}
          loading={loading}
        />
      )}
    </div>
  )
}
```

- [ ] **Step 2: Create src/components/ directory**

```bash
mkdir -p /Users/sangjun/IdeaProjects/mailer/src/components
```

- [ ] **Step 3: Commit**

```bash
git add src/App.jsx src/components/
git commit -m "feat: App.jsx with auth gate and job list"
```

---

### Task 11: JobCard.jsx

**Files:**
- Create: `/Users/sangjun/IdeaProjects/mailer/src/components/JobCard.jsx`

- [ ] **Step 1: Create JobCard.jsx**

```jsx
// src/components/JobCard.jsx
import { Play, Square, Pencil, Trash2 } from 'lucide-react'

function fmt(iso) {
  if (!iso) return '미발송'
  return new Date(iso).toLocaleString('ko-KR', {
    month: 'short', day: 'numeric',
    hour: '2-digit', minute: '2-digit',
  })
}

function intervalLabel(minutes) {
  if (minutes < 60) return `${minutes}분마다`
  if (minutes % 60 === 0) return `${minutes / 60}시간마다`
  return `${minutes}분마다`
}

export default function JobCard({ job, onToggle, onEdit, onDelete }) {
  return (
    <div className={`job-card${job.is_active ? ' job-card-active' : ''}`}>
      <div className="job-card-top">
        <div>
          <div className="job-card-name">{job.name}</div>
        </div>
        <div className="job-card-actions">
          <button
            className={`btn ${job.is_active ? 'btn-stop' : 'btn-start'}`}
            onClick={onToggle}
          >
            {job.is_active ? <><Square size={11} /> 중지</> : <><Play size={11} /> 시작</>}
          </button>
          <button className="btn btn-edit" onClick={onEdit}>
            <Pencil size={11} /> 수정
          </button>
          <button className="btn btn-delete" onClick={onDelete}>
            <Trash2 size={11} />
          </button>
        </div>
      </div>

      <div className="job-card-meta">
        <span className="job-meta-item">
          <span className={`job-badge ${job.sender === 'gmail' ? 'job-badge-gmail' : 'job-badge-ms'}`}>
            {job.sender === 'gmail' ? 'Gmail' : 'Outlook'}
          </span>
        </span>
        <span className="job-meta-item">수신자 {job.recipients.length}명</span>
        <span className="job-meta-item">{intervalLabel(job.interval_minutes)}</span>
        <span className="job-meta-item">
          <span className={`job-status-dot${job.is_active ? ' job-status-dot-active' : ''}`} />
          {job.is_active ? '실행 중' : '중지됨'}
        </span>
        <span className="job-meta-item">누적 {job.send_count}회</span>
        <span className="job-meta-item">마지막: {fmt(job.last_sent_at)}</span>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/JobCard.jsx
git commit -m "feat: JobCard component"
```

---

### Task 12: TagInput.jsx

**Files:**
- Create: `/Users/sangjun/IdeaProjects/mailer/src/components/TagInput.jsx`

- [ ] **Step 1: Create TagInput.jsx**

```jsx
// src/components/TagInput.jsx
import { useState } from 'react'
import { X } from 'lucide-react'

export default function TagInput({ values, onChange, placeholder = '이메일 입력 후 Enter' }) {
  const [input, setInput] = useState('')

  const add = (raw) => {
    const email = raw.trim().replace(/,$/, '')
    if (!email || values.includes(email)) return
    onChange([...values, email])
  }

  const remove = (email) => onChange(values.filter(e => e !== email))

  const handleKeyDown = (e) => {
    if (e.key === 'Enter' || e.key === ',') {
      e.preventDefault()
      add(input)
      setInput('')
    } else if (e.key === 'Backspace' && !input && values.length > 0) {
      remove(values[values.length - 1])
    }
  }

  const handleBlur = () => {
    if (input) { add(input); setInput('') }
  }

  return (
    <div className="tag-input-wrap" onClick={e => e.currentTarget.querySelector('input')?.focus()}>
      {values.map(email => (
        <span key={email} className="tag-pill">
          {email}
          <button className="tag-pill-remove" onClick={() => remove(email)} type="button">
            <X size={10} />
          </button>
        </span>
      ))}
      <input
        className="tag-input-inner"
        value={input}
        onChange={e => setInput(e.target.value)}
        onKeyDown={handleKeyDown}
        onBlur={handleBlur}
        placeholder={values.length === 0 ? placeholder : ''}
      />
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/TagInput.jsx
git commit -m "feat: TagInput component for recipients"
```

---

### Task 13: JobModal.jsx

**Files:**
- Create: `/Users/sangjun/IdeaProjects/mailer/src/components/JobModal.jsx`

- [ ] **Step 1: Create JobModal.jsx**

```jsx
// src/components/JobModal.jsx
import { useState } from 'react'
import TagInput from './TagInput.jsx'

export default function JobModal({ job, onSubmit, onClose, loading }) {
  const [name, setName] = useState(job?.name ?? '')
  const [sender, setSender] = useState(job?.sender ?? 'gmail')
  const [subject, setSubject] = useState(job?.subject ?? '')
  const [body, setBody] = useState(job?.body ?? '')
  const [recipients, setRecipients] = useState(job?.recipients ?? [])
  const [intervalValue, setIntervalValue] = useState(() => {
    if (!job) return 60
    return job.interval_minutes >= 60 && job.interval_minutes % 60 === 0
      ? job.interval_minutes / 60
      : job.interval_minutes
  })
  const [intervalUnit, setIntervalUnit] = useState(() => {
    if (!job) return 'hours'
    return job.interval_minutes >= 60 && job.interval_minutes % 60 === 0 ? 'hours' : 'minutes'
  })

  const handleSubmit = (e) => {
    e.preventDefault()
    const interval_minutes = intervalUnit === 'hours'
      ? Number(intervalValue) * 60
      : Number(intervalValue)
    onSubmit({ name, sender, subject, body, recipients, interval_minutes })
  }

  return (
    <div className="modal-overlay" onClick={e => { if (e.target === e.currentTarget) onClose() }}>
      <div className="modal">
        <h2 className="modal-title">{job ? '작업 수정' : '새 작업'}</h2>
        <form onSubmit={handleSubmit}>
          <div className="form-field">
            <label className="form-label">작업 이름</label>
            <input
              className="form-input"
              value={name}
              onChange={e => setName(e.target.value)}
              placeholder="예: 주간 리포트 발송"
              required
            />
          </div>

          <div className="form-field">
            <label className="form-label">발신자</label>
            <div className="radio-group">
              <label className="radio-label">
                <input type="radio" value="gmail" checked={sender === 'gmail'} onChange={() => setSender('gmail')} />
                Gmail
              </label>
              <label className="radio-label">
                <input type="radio" value="ms" checked={sender === 'ms'} onChange={() => setSender('ms')} />
                Outlook
              </label>
            </div>
          </div>

          <div className="form-field">
            <label className="form-label">메일 제목</label>
            <input
              className="form-input"
              value={subject}
              onChange={e => setSubject(e.target.value)}
              placeholder="메일 제목"
              required
            />
          </div>

          <div className="form-field">
            <label className="form-label">메일 본문</label>
            <textarea
              className="form-textarea"
              value={body}
              onChange={e => setBody(e.target.value)}
              placeholder="메일 내용을 입력하세요"
              required
            />
          </div>

          <div className="form-field">
            <label className="form-label">수신자</label>
            <TagInput values={recipients} onChange={setRecipients} />
          </div>

          <div className="form-field">
            <label className="form-label">발송 간격</label>
            <div className="interval-row">
              <input
                className="form-input"
                type="number"
                min="1"
                value={intervalValue}
                onChange={e => setIntervalValue(e.target.value)}
                required
              />
              <select
                className="form-select"
                value={intervalUnit}
                onChange={e => setIntervalUnit(e.target.value)}
              >
                <option value="minutes">분</option>
                <option value="hours">시간</option>
              </select>
            </div>
          </div>

          <div className="modal-actions">
            <button type="button" className="modal-cancel" onClick={onClose}>취소</button>
            <button
              type="submit"
              className="modal-submit"
              disabled={loading || recipients.length === 0}
            >
              {loading ? '저장 중...' : (job ? '수정' : '생성')}
            </button>
          </div>
        </form>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/JobModal.jsx
git commit -m "feat: JobModal form for create/edit"
```

- [ ] **Step 3: Verify full dev build**

```bash
cd /Users/sangjun/IdeaProjects/mailer
npm run dev
```

브라우저에서 `http://localhost:5173` 확인:
1. 비밀번호 입력 화면 표시됨
2. APP_PASSWORD 입력 시 메인 화면으로 전환
3. `+ 새 작업` 클릭 시 모달 열림
4. 작업 생성 → 목록에 표시됨
5. 시작 버튼 클릭 시 카드 테두리가 보라색으로 변함

---

### Task 14: Deploy to Vercel

**Files:** 없음 (Vercel 배포)

- [ ] **Step 1: Push to GitHub**

GitHub에서 `mailer` 레포 생성 후:

```bash
cd /Users/sangjun/IdeaProjects/mailer
git remote add origin https://github.com/<username>/mailer.git
git push -u origin main
```

- [ ] **Step 2: Create Vercel project**

Vercel Dashboard → New Project → Import Git Repository → `mailer` 선택.

Framework Preset: `Vite` 선택.

- [ ] **Step 3: Set environment variables in Vercel**

Vercel Project Settings → Environment Variables:

| Key | Value |
|-----|-------|
| `VITE_MAILER_URL` | `https://enawzdqroidrhtjqhpka.supabase.co/functions/v1/mailer` |
| `VITE_SUPABASE_ANON_KEY` | `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVuYXd6ZHFyb2lkcmh0anFocGthIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjkwMjM1MTIsImV4cCI6MjA4NDU5OTUxMn0.3D7Zeop7MzA-AytZw07hCjN7MddeWK_gN4aQY5F3AEg` |

- [ ] **Step 4: Deploy**

Vercel → Deploy 클릭. 배포 완료 후 제공된 URL 확인.

- [ ] **Step 5: End-to-end test**

1. 배포된 URL 접속 → 비밀번호 게이트 표시
2. 비밀번호 입력 → 메인 화면
3. 새 작업 생성 (1분 간격, 수신자 본인 이메일)
4. 시작 클릭
5. 2분 대기 후 메일 수신 확인
6. Supabase Dashboard → `mail_jobs` 테이블에서 `send_count` 증가 확인

---

## Self-Review

**스펙 커버리지 체크:**
- ✅ DB 스키마 (Task 1)
- ✅ Edge Function CRUD + tick (Task 2-4)
- ✅ Gmail + Outlook SMTP (Task 3)
- ✅ Supabase Secrets (Task 5)
- ✅ pg_cron 1분 주기 (Task 6)
- ✅ 프론트 비밀번호 게이트 (Task 10)
- ✅ 작업 목록 + 카드 (Task 11)
- ✅ 수신자 태그 입력 (Task 12)
- ✅ 작업 생성/수정 모달 (Task 13)
- ✅ Vercel 배포 (Task 14)

**주의사항:**
- Task 5에서 실제 Gmail/Outlook App Password 값을 반드시 넣어야 함
- Gmail App Password는 Google 계정 → 보안 → 2단계 인증 켠 후 생성 가능
- pg_net 확장이 `parking` 프로젝트에서 활성화되어 있어야 pg_cron이 HTTP 호출 가능
